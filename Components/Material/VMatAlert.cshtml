@inherits VMatAlertCode

@if (Visible) {
    <div class="@alertClass" role="alert">
        @Text
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
    </div>
}

